#!/bin/sh

const i: number = 1.2;
const flag = 0b0011;

const lastName = "jules";
// const fullName = `${ lastName }`;

class Cat {}
const cat: Cat = null;

if (i > 0) {
  console.log('"i" is positive.');
}